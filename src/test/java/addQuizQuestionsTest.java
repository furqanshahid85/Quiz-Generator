import static org.junit.Assert.*;

import org.junit.Test;


// this test is for checking that which question type is selected 

public class addQuizQuestionsTest {

	@Test
	public void test() {
		boolean rdbm=true;
		boolean rdbtf=true;
		boolean rdbbl=true;
		assertEquals(true,rdbm);
		assertEquals(true,rdbtf);
		assertEquals(true,rdbbl);
		
	}

}
